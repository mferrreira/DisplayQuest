/**
 * COMPAT RE-EXPORT (E1/T1.3): single sonner-backed implementation lives in
 * contexts/use-toast.ts. New code should import { toast } from "sonner" directly.
 */
export { useToast, toast } from "@/contexts/use-toast"
