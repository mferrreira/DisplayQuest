 for this project, i have a few updates, some minor, some very major changes. 

- Deeper gamification: The current PBL is way too shallow, i must think of better ways to promote and implement gamification, maybe by implementing experience, lootboxes, levels, tiers/ELOs (like leake of legends), and these must stay in the gamification domain. Also, gamification used to be user-specific, but these applications must also be project-specific and interact with the user gamification domain (still don't know how).
- Deeper Project changes: A project is currently basic, while in the institution, the project is way more complex, having a brochure, chronogram with relative periods for major tasks, the major tasks progress must be tracked (as well as the time that members takes to finish them -- being rewarded if done earlier and punished if done later). Also, daily logs and work sessions must interact with these new domains in a more concise way. The main goal on this is to be able to generate a pdf (template-specific, i must provide later) populated for each user on different time frames (week, month, semester, year and whole project). 
- Logging and audit: I must think of an easy way to implement logging without having to go on each domain, function and insertion to log, maybe on a higher stage near the composition root. 
- Roles and permissions: Currently, only managers can approve tasks, but the lab reality is more complicated than that. The manager barely shows on the office and the coordinator doesn't want to use the system. To solve this, i had to grant manager access to project leaders, but this is not right. Maybe, project leaders can approve the tasks from the staff in his project, but only managers can approve their own tasks (i, as a project leader, can create tasks for others and approve them. But if i create a task to myself, only a manager can approve it).
- The next step: Multi-lab capability: The current system runs a container for a whole lab. I want this to be a more SaaS approach, but with a lower-level thinking -- We have a specific way to grant a temporary endpoind (or single use endpoint) for new lab coordinators to create their own lab and account, and only then, access the system via the url hisNewLabName.displayquest.ifnmg.edu.br (*.displayquest.ifnmg.edu.br). A coordinator can be part of other labs, but can be the owner of his own lab as well. For this, the system launches a new docker-compose or something, containing the system infrastructure, but suited and populated with the data that the new coordinador provided for the new lab instance. this needs a lower-level access and is a very delicated use case. 
- New Auth SSO/LDAP: I need to create a way to detach auth and user domains, as well as create a new LDAP or SSO based way to connect this system and other services from the lab. See, in this new LDAP/SSO process (must be a different project, but communicate with the lab resources/projects/platforms), we have a level of permission and RBAC, but it's not necessarily the same as the used in DisplayQuest or other projects of the lab. I can give you the url for other project if you want to investigate it and come to a new solution. 


These are not all of them, but the biggest and most important ones. What i want you do to is create a plan to create plans, basically. You'll create a folder named plan-v2 in the project (dev branch) and for each new functionality, create a subfolder with their names and the order that they need to be implemented. Inside each subfolder, must have a PLAN.md, SPEC.md, AGENT.md and a STATE.json. on the plan-v2 folder, also include an .md architecture and state.json which will be used to track the progress of the implementations. always, and i mean, ALWAYS, respect the project architecture when introducing new codes, generate rigorous done criteria and only proceed if they are all completely achieved and tested. If somethings breaks, it must back off, rollback, think again and redo what's broke until it's done. This will be a SPEC-Driven development with state management. any questions, you can always ask me and search the web for best practices, design patterns and these things. 
